{
  "name": "company_app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start-react": "cd comp_app_front && npm start",
    "start-server": "cd comp_app_back && node index.js",
    "begin": "concurrently --kill-others \"npm run start-server\" \"npm run start-react\""
  },
  "author": "Elton Lucien",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "concurrently": "^7.5.0"
  }
}

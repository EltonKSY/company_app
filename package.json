{
  "name": "company_app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start-react": "cd comp_app_front && npm start",
    "start-server": "cd comp_app_back && npm run dev",
    "begin": "concurrently --kill-others \"npm run start-server\" \"npm run start-react\""
  },
  "author": "Elton Lucien",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.5.0"
  }
}
